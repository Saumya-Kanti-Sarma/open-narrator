1. use modular css.
2. break code into modularity. Follow the code architecture:
  - components : sidebar, navbar etc
  - UI (elements) : buttons, searchbar etc
  - utils
  - service
3. write easy to understand code.
4. Do not cluster more then one component in one file